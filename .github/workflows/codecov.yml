name: Code Coverage

on:
  pull_request:
    branches: [ master ]

jobs:
  run:
    name: Report Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Upload Coverage to Codecov
        uses: ./
        with:
          flags: unittest
          name: codecov-1
