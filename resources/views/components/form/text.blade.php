<div class="form-group">
    <label for="{{ $name }}" class="control-label">{{ $name }}</label>
    <input type="text" name="{{ $name }}" value="{{ $value }}" class="form-control" {{ $attributes }}>
</div>
