<div id="page-heading">
    <div>
        <?php if (isset($pageBreadcrumb)) { ?>
            <?php foreach ($pageBreadcrumb as $bc) { ?>
                <a href="<?=site_url($bc["link"])?>"><?= $bc["title"] ?></a> &gt;
            <?php } ?>
        <?php } ?>
        <?= $pageTitle ?>
    </div>
    <h1>
        <?php if (isset($pageTitle)) { ?>
            <?= $pageTitle ?>
        <?php } ?>
    </h1>
</div>
